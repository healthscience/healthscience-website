# Plan: Tri-Lens Navigation Redesign & Poetry Lens Implementation

## Objectives
1.  **HUD Evolution**: Completed. Persistent top bar (HUB) and Tri-Axis indicator implemented.
2.  **Poetry Lens Implementation**: Refactor `hs-lens-poetry.js` to serve as a "Boreal/Nature" manifesto lens, porting content and aesthetics from the `v2` branch (Nuxt-based version).
3.  **Visual Consistency**: Maintain the "Spiral Background" and "Techno-Vine" aesthetic for the Poetry lens.

## Implementation Details for Poetry Lens

### 1. Aesthetic Port (from `v2`)
- **Background**: Replicate the `SpiralBackground.vue` logic using SVG/CSS within the lens.
- **Color Scheme**: Use the "Nature" palette from `v2`:
    - Forest Void: `#0a0f0d` (Dark background)
    - Neon Pulse: `#a9ff00` (Accent)
    - Tactical Pine: `#2d4635`
- **Ornaments**: Replicate "Daisy nodes" and "Leaf pairs" (vines) as decorative elements using CSS classes based on `nature.css`.

### 2. Content Structure (The Manifesto)
The lens will be a scrollable narrative containing:
- **Hero**: Gaia intelligences shape health.
- **Small Intelligences**: Molecular, Cellular, and Mycelial scales.
- **Cues & Besearch**: The Metabolic Logic of Shannon-Entropy and Active Inference.
- **Metabolic Journey**: Diagrammatic explanation of the HOP Fabric (SafeFlow-ECS).
- **The Boreal Horizon**: The Nature of Computation (Alignment, Conformity, Synthesis).

### 3. Technical Approach
- **Vanilla Component**: Since the current app is Vanilla JS, I will port the Vue components (`HeroModule`, `CuesBesearch`, etc.) into a single cohesive HTML/CSS structure inside `hs-lens-poetry.js`.
- **CSS Transitions**: Ensure smooth "Word as Medicine" typography (Serif for narrative, Mono for tech labels).

## Implementation Steps

### Step 1: Prepare CSS in `hs-lens-poetry.js`
- Port the `@keyframes spin` and spiral SVG definitions.
- Port the `.daisy-node`, `.leaf-pair`, and `.lego-module` styles from `nature.css`.

### Step 2: Build the HTML Structure
- Create a container with `overflow-y: auto` and `scroll-snap-type`.
- Implement sections for Hero, Intelligences, Cues, and Boreal Horizon.

### Step 3: Refine Emulation Lens
- Ensure `hs-lens-emulation.js` doesn't conflict with the new Poetry styles (use Shadow DOM encapsulation strictly).

### Step 4: Verification
- Verify that the Poetry lens is readable and the "Spirals" animate smoothly.
- Ensure the Heli-Clock in Emulation lens remains properly positioned below the HUD.
