# Plan: Upgrade Skeleton Lens to "Repository of Truth"

This plan upgrades the `hs-lens-skeleton.js` component to reflect the "Repository of Truth" and "PeerStack" sections from healthscience.network. It will feature a technical, low-level aesthetic with permalinks to playgrounds.

## Proposed Changes

### 1. Structure `hs-lens-skeleton.js`
- **Layout**: Keep the terminal/engine room feel but expand it into a full scrolling technical document.
- **Header**: "THE REPOSITORY OF TRUTH"
- **Sections**:
    - **I. Gaia as Systems Physics**: Mathematical recognition of nested systems.
    - **II. The "Gaming Engine for Life" (BentoBoxDS)**: Dynamic state-machine logic.
    - **PROTOCOL Roadmap**: Modular grid of technical components (BentoBoxDS, BeeBee, Besearch, etc.).
    - **PILLARS OF THE PROTOCOL**: Deep dives into Gaia Intelligences, etc.
- **Playground Links**: Integrate permalinks (placeholders like `#emulation/bento`) for each technical concept.

### 2. Styling
- **Color Palette**: Stick to the `--color-skeleton` (likely a vibrant green or matrix-like color) on black.
- **Typography**: Heavy use of monospace, code blocks, and technical diagrams (SVG or ASCII-art style).
- **Navigation**: Internal sub-navigation or "grafting" points that respond to `subState`.

### 3. Navigation Handling
- Update `onActivate(subState)` to scroll to specific sections if a permalink is used (e.g., `#skeleton/bentobox`).

## Technical Details

- **File**: `js/components/hs-lens-skeleton.js`
- **Components to implement**:
    - `renderSections()`: To generate the structured content provided in the prompt.
    - `renderRoadmap()`: A grid/map of the protocol modules.
    - `renderPillars()`: Detailed technical breakdowns.

## Verification Plan

- **Visual Check**: Ensure the new sections are readable and match the "Skeleton" aesthetic (technical, high-contrast).
- **Navigation Check**: Verify that `window.location.hash` changes correctly and that the lens responds to sub-states.
- **Build**: Ensure no JS errors.
